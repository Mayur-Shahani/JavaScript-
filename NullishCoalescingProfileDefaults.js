const apiData = {
    username: null ?? "Mayur",
    bio: undefined ?? "Not Available",
    followers: 0,
    posts: null ?? [3],
    theme: undefined ?? "dark",
    
    
};
console.log(`Username is ${apiData.username} and bio is ${apiData.bio}. The Followers is ${apiData.followers}. Total Posts is ${apiData.posts} and theme is ${apiData.theme}.`);